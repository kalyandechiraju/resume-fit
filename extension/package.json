{
  "name": "@resume-fit/extension",
  "version": "0.3.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "0.2.8",
    "esbuild": "0.28.2",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^4.0.87",
    "@ai-sdk/typesafe-ai": "3.0.4",
    "ai": "^7.0.107",
    "fflate": "^0.8.2",
    "unpdf": "^1.8.1"
  }
}
